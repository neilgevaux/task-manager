
  <div class="card" style="width: 42rem">
    <div class="card-body">
      <div class="form-group">
        <%= form_with(model: task_list) do |form| %>
          <% if task_list.errors.any? %>
            <div style="color: red">
              <h2><%= pluralize(task_list.errors.count, "error") %> prohibited this task_list from being saved:</h2>

              <ul>
                <% task_list.errors.each do |error| %>
                  <li><%= error.full_message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div>
            <%= form.label :title, style: "display: block" %>
            <%= form.text_field :title, placeholder: "e.g. Urgent and important", class: "form-control" %>
          </div>

          <div>
            <%= form.label :description, style: "display: block" %>
            <%= form.text_area :description, placeholder: "e.g. Add your primary tasks here", class: "form-control" %>
          </div>
          <br/>
          <div>
            <%= form.submit "Create bucket", class: "btn btn-primary" %>
          </div>
          <% end %>
      </div><!-- End form group -->
    </div><!-- End card body -->
  </div><!-- End card -->